This C code generates a .wav file of required frequency,volume and duration.

To run this code:
use the makefile using the "make" command.

Enter arguments as follow: ./mytry duration frequency volume filename.wav